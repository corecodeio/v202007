import React from "react";
import { MainActionButton } from "../../../common/component/MainActionButton";

export const SendPhoneNumberVerificationCode: React.FC = () => {
  return (
    <MainActionButton
      bg="#000"
      p={21}
      m={23}
      title="MainActionButton"
      titleColor="#fff"
    />
  );
};

export default SendPhoneNumberVerificationCode;
