import React from "react";
import { Text, TextInput, View } from "react-native";

export const OnboardingStoreConnection: React.FC<{}> = () => {
	return (
		<View>
			<Text>Now connect to your store</Text>
			<TextInput></TextInput>
		</View>
	);
};
